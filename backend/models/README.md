Place trained model checkpoints here, e.g. `phytosense_fusion.pt`,
produced by running:

```bash
python -m app.ml.train --manifest ../data/manifest.csv --epochs 20
```

This directory is git-ignored for the actual `.pt` files to keep the repo small.
